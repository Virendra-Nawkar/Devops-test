{
  "DL3000": {
    "title": "Use absolute path for WORKDIR",
    "severity": "MEDIUM",
    "tool": "hadolint",
    "why": "A relative WORKDIR is ambiguous and depends on where the previous WORKDIR left off. Future maintainers (and tools) may misread the directory the container is actually running in.",
    "fix": "Always use an absolute path:\n  # Bad:  WORKDIR app\n  # Good: WORKDIR /app",
    "learnMore": "Study: Docker WORKDIR instruction, Linux absolute vs relative paths"
  },
  "DL3003": {
    "title": "Use absolute path for WORKDIR",
    "severity": "MEDIUM",
    "tool": "hadolint",
    "why": "A relative WORKDIR (e.g. WORKDIR app) is resolved against whatever directory was set before it. This is error-prone and hard to reason about.",
    "fix": "Use an absolute path starting with /:\n  WORKDIR /app",
    "learnMore": "Study: Docker WORKDIR, Dockerfile best practices"
  },
  "DL3004": {
    "title": "Do not use sudo inside a Dockerfile",
    "severity": "HIGH",
    "tool": "hadolint",
    "why": "sudo may not be installed in the base image, and using it is a sign that the container is running as a non-privileged user trying to gain root. Better to run the RUN commands as root during build and switch to a non-root USER at the end.",
    "fix": "Remove sudo — RUN commands execute as root by default during build:\n  # Bad:  RUN sudo apt-get install -y curl\n  # Good: RUN apt-get install -y curl",
    "learnMore": "Study: Docker build context, RUN as root vs runtime USER"
  },
  "DL3011": {
    "title": "Invalid port number — must be between 0 and 65535",
    "severity": "HIGH",
    "tool": "hadolint",
    "why": "TCP/UDP port numbers are 16-bit unsigned integers (0–65535). Exposing a port outside this range is invalid and will cause runtime errors.",
    "fix": "Change the EXPOSE instruction to a valid port number:\n  EXPOSE 3000",
    "learnMore": "Study: TCP/UDP port numbers, IANA port registry"
  },
  "DL3012": {
    "title": "Multiple consecutive RUN instructions — combine them",
    "severity": "LOW",
    "tool": "hadolint",
    "why": "Each RUN instruction creates a new image layer. Multiple consecutive RUN instructions bloat the image size because intermediate layers cannot be removed.",
    "fix": "Combine RUN instructions using && and backslash continuation:\n  # Bad:\n  RUN apt-get update\n  RUN apt-get install -y curl\n  RUN rm -rf /var/lib/apt/lists/*\n\n  # Good:\n  RUN apt-get update \\\n   && apt-get install -y curl \\\n   && rm -rf /var/lib/apt/lists/*",
    "learnMore": "Study: Docker image layers, layer caching, image optimisation"
  },
  "DL3013": {
    "title": "Pin versions in pip install",
    "severity": "MEDIUM",
    "tool": "hadolint",
    "why": "Without pinned versions, pip will install the latest version on every build. A new release of a dependency can break your app silently.",
    "fix": "Pin every package to an exact version:\n  # Bad:  RUN pip3 install flask requests\n  # Good: RUN pip3 install flask==3.0.0 requests==2.31.0",
    "learnMore": "Study: Python dependency pinning, pip freeze, requirements.txt, virtual environments"
  },
  "DL3016": {
    "title": "Pin versions in npm install",
    "severity": "MEDIUM",
    "tool": "hadolint",
    "why": "npm install without a lock file or pinned versions pulls the latest compatible release. This makes builds non-reproducible.",
    "fix": "Use npm ci (which respects package-lock.json) instead of npm install:\n  # Bad:  RUN npm install\n  # Good: RUN npm ci --only=production",
    "learnMore": "Study: npm ci vs npm install, package-lock.json, reproducible builds"
  },
  "DL3017": {
    "title": "Do not use apk upgrade",
    "severity": "MEDIUM",
    "tool": "hadolint",
    "why": "apk upgrade upgrades all packages to the latest version, making the build non-reproducible. Two builds on different days will install different package versions.",
    "fix": "Remove apk upgrade. Pin specific versions with apk add instead:\n  # Bad:  RUN apk upgrade && apk add curl\n  # Good: RUN apk add --no-cache curl=8.x.x",
    "learnMore": "Study: Alpine Linux apk, reproducible builds, immutable infrastructure"
  },
  "DL3018": {
    "title": "Pin versions in apk add",
    "severity": "MEDIUM",
    "tool": "hadolint",
    "why": "Without a pinned version, apk add pulls the latest package on every build. The package version can change silently, introducing bugs or vulnerabilities.",
    "fix": "Pin the version with =:\n  # Bad:  RUN apk add --no-cache curl\n  # Good: RUN apk add --no-cache curl=8.9.1-r0",
    "learnMore": "Study: Alpine Linux package pinning, apk version syntax"
  },
  "DL3019": {
    "title": "Use --no-cache with apk add",
    "severity": "LOW",
    "tool": "hadolint",
    "why": "Without --no-cache, apk stores the package index on disk. This adds unnecessary bytes to the image layer.",
    "fix": "Add --no-cache flag (replaces the need for apk update + cache cleanup):\n  # Bad:  RUN apk update && apk add curl && rm -rf /var/cache/apk/*\n  # Good: RUN apk add --no-cache curl",
    "learnMore": "Study: Alpine Linux image optimisation, Docker layer size"
  },
  "DL3021": {
    "title": "COPY with multiple sources requires destination to end with /",
    "severity": "HIGH",
    "tool": "hadolint",
    "why": "When copying multiple files into a directory, the destination must be a directory path (ending with /). Without the slash, Docker interprets the last argument as a filename and the copy fails.",
    "fix": "End the destination path with a slash:\n  # Bad:  COPY file1.txt file2.txt /app\n  # Good: COPY file1.txt file2.txt /app/",
    "learnMore": "Study: Docker COPY syntax, source/destination paths"
  },
  "DL3022": {
    "title": "COPY --from should reference a previously defined FROM alias",
    "severity": "MEDIUM",
    "tool": "hadolint",
    "why": "COPY --from references a build stage by name. If the alias doesn't match a FROM ... AS alias line, the build will fail.",
    "fix": "Name your build stages and reference them exactly:\n  FROM node:20-alpine AS builder\n  ...\n  FROM node:20-alpine\n  COPY --from=builder /app/dist /app/dist",
    "learnMore": "Study: Docker multi-stage builds, FROM AS alias"
  },
  "DL3026": {
    "title": "Use only an allowed base image registry",
    "severity": "MEDIUM",
    "tool": "hadolint",
    "why": "Pulling from unofficial or unknown registries can introduce malicious images. Your organisation may have a policy that allows only specific trusted registries.",
    "fix": "Use images from your approved registry list:\n  # Example: only use docker.io or your internal registry\n  FROM docker.io/node:20-alpine\n  # Or: FROM registry.company.com/node:20-alpine",
    "learnMore": "Study: Container registry security, supply chain attacks, image provenance"
  },
  "DL3027": {
    "title": "Do not use apt — use apt-get instead",
    "severity": "LOW",
    "tool": "hadolint",
    "why": "apt is designed for interactive terminal use and may produce different output or behaviour in non-interactive scripts. apt-get is the stable, scriptable interface.",
    "fix": "Replace apt with apt-get:\n  # Bad:  RUN apt install -y curl\n  # Good: RUN apt-get install -y curl",
    "learnMore": "Study: Debian apt vs apt-get, non-interactive package installation"
  },
  "DL3028": {
    "title": "Pin versions in gem install",
    "severity": "MEDIUM",
    "tool": "hadolint",
    "why": "Without pinned versions, gem install pulls the latest gem on every build, making builds non-reproducible and potentially introducing breaking changes.",
    "fix": "Pin the gem version:\n  # Bad:  RUN gem install rails\n  # Good: RUN gem install rails -v 7.1.0",
    "learnMore": "Study: Ruby gem versioning, Gemfile.lock, Bundler"
  },
  "DL4000": {
    "title": "MAINTAINER is deprecated",
    "severity": "LOW",
    "tool": "hadolint",
    "why": "The MAINTAINER instruction is deprecated since Docker 1.13. It does not add metadata in a standard way and will be ignored by modern tools.",
    "fix": "Remove MAINTAINER and use a LABEL instead:\n  # Bad:  MAINTAINER John Doe <john@example.com>\n  # Good: LABEL maintainer=\"John Doe <john@example.com>\"",
    "learnMore": "Study: Docker LABEL instruction, OCI image annotations"
  },
  "DL4001": {
    "title": "Use either wget or curl — not both",
    "severity": "LOW",
    "tool": "hadolint",
    "why": "Installing both wget and curl adds unnecessary packages to the image. Each extra package increases image size and attack surface. Pick one and use it consistently.",
    "fix": "Choose one tool and remove the other:\n  # Pick curl:\n  RUN apt-get install -y --no-install-recommends curl \\\n   && rm -rf /var/lib/apt/lists/*",
    "learnMore": "Study: Docker image minimisation, attack surface reduction"
  },
  "DL4003": {
    "title": "Multiple CMD instructions found",
    "severity": "HIGH",
    "tool": "hadolint",
    "why": "Only the last CMD instruction is used. Having multiple CMD instructions is misleading and suggests a misunderstanding of how Dockerfiles work.",
    "fix": "Keep only one CMD instruction at the end of the Dockerfile:\n  CMD [\"node\", \"server.js\"]",
    "learnMore": "Study: Docker CMD vs ENTRYPOINT, Dockerfile instruction reference"
  },
  "DL4004": {
    "title": "Multiple ENTRYPOINT instructions found",
    "severity": "HIGH",
    "tool": "hadolint",
    "why": "Only the last ENTRYPOINT is used. Having multiple ENTRYPOINT instructions is a bug — the first ones are silently ignored.",
    "fix": "Keep only one ENTRYPOINT instruction:\n  ENTRYPOINT [\"node\", \"server.js\"]",
    "learnMore": "Study: Docker ENTRYPOINT, CMD vs ENTRYPOINT"
  },
  "DL4005": {
    "title": "Use SHELL to set default shell",
    "severity": "LOW",
    "tool": "hadolint",
    "why": "The default shell in Dockerfile RUN commands is /bin/sh -c, which does not support features like arrays or advanced bash syntax. If your RUN commands require bash, set the SHELL explicitly.",
    "fix": "Set the shell before using bash features:\n  SHELL [\"/bin/bash\", \"-o\", \"pipefail\", \"-c\"]\n  RUN my-bash-script.sh",
    "learnMore": "Study: Docker SHELL instruction, bash vs sh differences"
  },
  "SC2086": {
    "title": "Double-quote variable to prevent word splitting",
    "severity": "LOW",
    "tool": "hadolint",
    "why": "In shell scripts, unquoted variables undergo word splitting and glob expansion. If a variable contains spaces or special characters, the command will behave unexpectedly.",
    "fix": "Always double-quote variables:\n  # Bad:  RUN echo $MY_VAR\n  # Good: RUN echo \"$MY_VAR\"",
    "learnMore": "Study: Bash word splitting, quoting rules, ShellCheck SC2086"
  },
  "SC2039": {
    "title": "Feature not supported in POSIX sh",
    "severity": "LOW",
    "tool": "hadolint",
    "why": "The Dockerfile default shell is /bin/sh (POSIX sh). Features like arrays, [[ ]], local, and source are bash-specific and will fail in sh.",
    "fix": "Either switch to bash with the SHELL instruction, or rewrite using POSIX sh syntax:\n  SHELL [\"/bin/bash\", \"-c\"]\n  RUN source /etc/profile",
    "learnMore": "Study: POSIX sh vs bash, Docker SHELL instruction, ShellCheck"
  },
  "SC2046": {
    "title": "Quote command substitution to prevent word splitting",
    "severity": "LOW",
    "tool": "hadolint",
    "why": "Unquoted command substitution $(cmd) is subject to word splitting. If the output contains spaces, the shell splits it into multiple arguments unexpectedly.",
    "fix": "Quote command substitutions:\n  # Bad:  RUN echo $(cat file.txt)\n  # Good: RUN echo \"$(cat file.txt)\"",
    "learnMore": "Study: Bash command substitution, word splitting, ShellCheck SC2046"
  },
  "DL3002": {
    "title": "Last USER should not be root",
    "severity": "HIGH",
    "tool": "hadolint",
    "why": "Running a container as root means if an attacker breaks out of the app, they have full control over the container and possibly the host. The principle of least privilege says: only give the minimum access needed.",
    "fix": "Add these two lines near the END of your Dockerfile:\n  RUN addgroup -S appgroup && adduser -S appuser -G appgroup\n  USER appuser",
    "learnMore": "Study: Linux user permissions, principle of least privilege, Docker security best practices"
  },
  "DL3008": {
    "title": "Pin versions in apt-get install",
    "severity": "MEDIUM",
    "tool": "hadolint",
    "why": "Without pinned versions, every time the image is built it may install a different version of a package. This makes builds non-reproducible and can introduce unexpected bugs or security vulnerabilities.",
    "fix": "Pin the version: instead of  RUN apt-get install -y curl  use  RUN apt-get install -y curl=7.68.0-1ubuntu2",
    "learnMore": "Study: Reproducible builds, semantic versioning, apt package pinning"
  },
  "DL3009": {
    "title": "Delete apt-get lists after install",
    "severity": "LOW",
    "tool": "hadolint",
    "why": "apt-get stores package index files in /var/lib/apt/lists/. These files are only needed during installation. Leaving them in the image wastes space (sometimes 50-100 MB).",
    "fix": "Always end your apt-get block with:\n  && rm -rf /var/lib/apt/lists/*\nFull example:\n  RUN apt-get update && apt-get install -y curl && rm -rf /var/lib/apt/lists/*",
    "learnMore": "Study: Docker image layer optimisation, multi-stage builds"
  },
  "DL3015": {
    "title": "Avoid installing unnecessary packages",
    "severity": "LOW",
    "tool": "hadolint",
    "why": "Packages you don't need increase the image size, add more potential CVEs to scan, and give attackers more tools if they break in.",
    "fix": "Use the --no-install-recommends flag:\n  RUN apt-get install -y --no-install-recommends curl",
    "learnMore": "Study: Docker image minimisation, distroless images, Alpine Linux"
  },
  "DL3025": {
    "title": "Use exec form for CMD and ENTRYPOINT",
    "severity": "MEDIUM",
    "tool": "hadolint",
    "why": "Shell form (CMD python app.py) spawns a shell process as PID 1. Exec form (CMD [\"python\", \"app.py\"]) runs your process as PID 1 directly. PID 1 receives OS signals like SIGTERM, so exec form lets Docker gracefully stop your app.",
    "fix": "Change shell form to exec form:\n  # Bad:  CMD python app.py\n  # Good: CMD [\"python\", \"app.py\"]",
    "learnMore": "Study: Linux signals, PID 1, graceful container shutdown"
  },
  "DL3006": {
    "title": "Always tag the FROM image",
    "severity": "MEDIUM",
    "tool": "hadolint",
    "why": "FROM ubuntu with no tag silently uses :latest. This means tomorrow's build might pull a different OS version and break your app. Always pin to a specific version.",
    "fix": "Add a specific version tag:\n  # Bad:  FROM ubuntu\n  # Good: FROM ubuntu:22.04",
    "learnMore": "Study: Reproducible builds, semantic versioning, Docker image tags"
  },
  "DL3007": {
    "title": "Avoid using :latest tag",
    "severity": "MEDIUM",
    "tool": "hadolint",
    "why": "The :latest tag is a floating pointer. It changes whenever the image maintainer pushes a new version. Your build will silently change behaviour, break, or introduce vulnerabilities.",
    "fix": "Pin to a specific version:\n  # Bad:  FROM node:latest\n  # Good: FROM node:20-alpine",
    "learnMore": "Study: Image tag pinning, supply chain security, reproducible builds"
  },
  "DL3014": {
    "title": "Use -y flag with apt-get",
    "severity": "LOW",
    "tool": "hadolint",
    "why": "Without -y, apt-get waits for user input to confirm installation. In a Docker build there is no user, so the build hangs forever and eventually times out.",
    "fix": "Always add -y:\n  RUN apt-get install -y curl",
    "learnMore": "Study: Non-interactive shell scripts, Docker build process"
  },
  "DL4006": {
    "title": "Set SHELL option -o pipefail",
    "severity": "MEDIUM",
    "tool": "hadolint",
    "why": "When you use pipes (|) in RUN commands, bash only returns the exit code of the LAST command. If an earlier command fails, the error is silently ignored and the build succeeds when it shouldn't.",
    "fix": "Add this before any RUN command that uses pipes:\n  SHELL [\"/bin/bash\", \"-o\", \"pipefail\", \"-c\"]\n  RUN curl https://example.com | tar xz",
    "learnMore": "Study: Bash pipefail, shell error handling, set -e"
  },
  "DL3001": {
    "title": "Avoid platform-specific commands",
    "severity": "INFO",
    "tool": "hadolint",
    "why": "Commands like apt-get are specific to Debian/Ubuntu. If someone builds your Dockerfile on a different base image or OS, the command will fail. Try to write portable commands or document the base OS requirement.",
    "fix": "Be explicit about the base image OS and document it:\n  FROM ubuntu:22.04\n  # Requires Debian/Ubuntu — uses apt-get",
    "learnMore": "Study: Docker base images, distroless containers, Alpine Linux"
  },
  "DL3010": {
    "title": "Use ADD for archives, not COPY",
    "severity": "LOW",
    "tool": "hadolint",
    "why": "ADD has special behaviour: it automatically extracts .tar.gz archives. COPY is a simple file copy with no magic. Using ADD for plain files is confusing because readers expect extraction behaviour.",
    "fix": "Use COPY for plain files, ADD only for archives you want auto-extracted:\n  COPY app.js /app/\n  ADD app.tar.gz /app/",
    "learnMore": "Study: Docker COPY vs ADD, Dockerfile best practices"
  },
  "DL3020": {
    "title": "Use COPY instead of ADD for plain files",
    "severity": "MEDIUM",
    "tool": "hadolint",
    "why": "ADD with a local file path does the same thing as COPY but hides the intent. ADD can also fetch from URLs, which is a security risk (no checksum validation). COPY is explicit and safe.",
    "fix": "Replace ADD with COPY for local files:\n  # Bad:  ADD ./src /app/src\n  # Good: COPY ./src /app/src",
    "learnMore": "Study: Docker Dockerfile best practices, supply chain security"
  },
  "AVD-AZU-0002": {
    "title": "Azure Storage Account not enforcing HTTPS",
    "severity": "HIGH",
    "tool": "tfsec",
    "why": "If HTTPS is not enforced, data can be transmitted in plaintext over HTTP. An attacker on the same network can read your data (man-in-the-middle attack).",
    "fix": "Set enable_https_traffic_only = true in your azurerm_storage_account resource:\n  resource \"azurerm_storage_account\" \"example\" {\n    enable_https_traffic_only = true\n  }",
    "learnMore": "Study: TLS/HTTPS, man-in-the-middle attacks, Azure storage security"
  },
  "AVD-AZU-0007": {
    "title": "Azure VM has open management ports",
    "severity": "CRITICAL",
    "tool": "tfsec",
    "why": "Ports like SSH (22) and RDP (3389) open to the internet (0.0.0.0/0) are constantly scanned by bots. They will attempt brute-force attacks within minutes of opening the port.",
    "fix": "Restrict source to your specific IP or VPN CIDR:\n  source_address_prefix = \"10.0.0.0/8\"  # internal network only\n  # NOT: source_address_prefix = \"0.0.0.0/0\"",
    "learnMore": "Study: Network security groups, least privilege networking, VPN/bastion hosts"
  },
  "AVD-AZU-0016": {
    "title": "Azure NSG allows unrestricted inbound traffic",
    "severity": "CRITICAL",
    "tool": "tfsec",
    "why": "A rule with source 0.0.0.0/0 (any IP) on any port allows the entire internet to reach your resource. This is almost never intentional and exposes your service to attacks.",
    "fix": "Change source_address_prefix from 0.0.0.0/0 to a specific CIDR:\n  source_address_prefix = \"10.0.0.0/16\"",
    "learnMore": "Study: Azure NSG rules, CIDR notation, zero-trust networking"
  },
  "AVD-AZU-0017": {
    "title": "Azure NSG allows unrestricted egress",
    "severity": "MEDIUM",
    "tool": "tfsec",
    "why": "Unrestricted outbound means a compromised VM can call home to attacker-controlled servers, exfiltrate data, or be used in DDoS attacks. Limiting egress is defence-in-depth.",
    "fix": "Restrict egress to known destinations:\n  direction = \"Outbound\"\n  destination_address_prefix = \"10.0.0.0/8\"  # internal only",
    "learnMore": "Study: Egress filtering, defence in depth, data exfiltration prevention"
  },
  "AVD-AWS-0001": {
    "title": "AWS S3 bucket is publicly accessible",
    "severity": "CRITICAL",
    "tool": "tfsec",
    "why": "A public S3 bucket means anyone on the internet can read (or write) your files. Thousands of data breaches have occurred because of misconfigured public S3 buckets.",
    "fix": "Block all public access:\n  resource \"aws_s3_bucket_public_access_block\" \"example\" {\n    bucket = aws_s3_bucket.example.id\n    block_public_acls   = true\n    block_public_policy = true\n  }",
    "learnMore": "Study: S3 bucket policies, AWS public access block, data breach case studies"
  },
  "AVD-AWS-0086": {
    "title": "AWS Security Group allows unrestricted ingress",
    "severity": "HIGH",
    "tool": "tfsec",
    "why": "A security group with 0.0.0.0/0 as source exposes your EC2 instances to the entire internet. Even if your app is not ready, attackers will probe any open port.",
    "fix": "Use specific CIDR blocks:\n  cidr_blocks = [\"10.0.0.0/16\"]  # VPC internal only\n  # Or specify your office IP: [\"203.0.113.50/32\"]",
    "learnMore": "Study: AWS VPC security groups, least privilege networking, CIDR notation"
  },
  "AVD-AWS-0107": {
    "title": "AWS RDS instance not encrypted",
    "severity": "HIGH",
    "tool": "tfsec",
    "why": "Unencrypted RDS means if someone gets physical or logical access to the storage, they can read your database files in plaintext. Encryption at rest protects against storage-level attacks.",
    "fix": "Enable encryption:\n  resource \"aws_db_instance\" \"example\" {\n    storage_encrypted = true\n    kms_key_id        = aws_kms_key.rds.arn\n  }",
    "learnMore": "Study: Encryption at rest, AWS KMS, database security"
  },
  "AVD-GCP-0012": {
    "title": "GCP compute instance has a public IP",
    "severity": "MEDIUM",
    "tool": "tfsec",
    "why": "A public IP makes the instance directly reachable from the internet. Without a load balancer or firewall in front, every port is potentially exposed.",
    "fix": "Remove the public IP and use a load balancer or Cloud NAT for outbound:\n  # Remove access_config {} block from network_interface to disable public IP",
    "learnMore": "Study: GCP VPC, Cloud NAT, private Google Access, zero-trust networking"
  },
  "CKV_DOCKER_2": {
    "title": "Ensure HEALTHCHECK is set in Dockerfile",
    "severity": "MEDIUM",
    "tool": "checkov",
    "why": "Without a HEALTHCHECK, Docker and Kubernetes cannot tell if your app is actually working. The container might be 'running' but serving errors. Health checks enable automatic restarts.",
    "fix": "Add a HEALTHCHECK instruction:\n  HEALTHCHECK --interval=30s --timeout=3s \\\n    CMD wget -qO- http://localhost:3000/health || exit 1",
    "learnMore": "Study: Docker HEALTHCHECK, Kubernetes liveness probes, container health monitoring"
  },
  "CKV_DOCKER_3": {
    "title": "Ensure container does not run as root user",
    "severity": "HIGH",
    "tool": "checkov",
    "why": "See DL3002 — running as root is dangerous because it gives an attacker maximum privileges inside the container.",
    "fix": "Create and switch to a non-root user:\n  RUN addgroup -S app && adduser -S app -G app\n  USER app",
    "learnMore": "Study: Linux user namespaces, container security, principle of least privilege"
  },
  "CKV_DOCKER_4": {
    "title": "Do not use ADD with remote URLs",
    "severity": "HIGH",
    "tool": "checkov",
    "why": "ADD fetches remote URLs without any signature or checksum verification. An attacker who intercepts the download (or compromises the remote server) can replace the file with malware.",
    "fix": "Use curl/wget with a checksum instead:\n  RUN curl -fsSL https://example.com/file.tar.gz -o /tmp/file.tar.gz \\\n    && echo \"expected-sha256  /tmp/file.tar.gz\" | sha256sum -c",
    "learnMore": "Study: Supply chain attacks, checksum verification, secure downloads"
  },
  "CKV_K8S_8": {
    "title": "Liveness probe should be configured",
    "severity": "MEDIUM",
    "tool": "checkov",
    "why": "Without a liveness probe, Kubernetes cannot detect if your app is stuck in a deadlock or infinite loop. The pod keeps running but serving nothing — users get errors until someone notices.",
    "fix": "Add a livenessProbe to your container spec:\n  livenessProbe:\n    httpGet:\n      path: /health\n      port: 3000\n    initialDelaySeconds: 15\n    periodSeconds: 20",
    "learnMore": "Study: Kubernetes probes, self-healing systems, container health monitoring"
  },
  "CKV_K8S_9": {
    "title": "Readiness probe should be configured",
    "severity": "MEDIUM",
    "tool": "checkov",
    "why": "Without a readiness probe, Kubernetes sends traffic to a pod before it is ready (e.g., still loading config or connecting to a database). Users see errors during startup or rolling updates.",
    "fix": "Add a readinessProbe:\n  readinessProbe:\n    httpGet:\n      path: /ready\n      port: 3000\n    initialDelaySeconds: 5\n    periodSeconds: 10",
    "learnMore": "Study: Kubernetes readiness vs liveness probes, rolling deployments, zero-downtime updates"
  },
  "CKV_K8S_10": {
    "title": "CPU limits should be set",
    "severity": "MEDIUM",
    "tool": "checkov",
    "why": "Without CPU limits, one misbehaving pod can consume all CPU on a node and starve other pods. This causes cascading failures across the cluster.",
    "fix": "Set CPU limits in your container resources:\n  resources:\n    limits:\n      cpu: \"500m\"     # 0.5 CPU cores",
    "learnMore": "Study: Kubernetes resource management, CPU throttling, QoS classes"
  },
  "CKV_K8S_11": {
    "title": "CPU requests should be set",
    "severity": "LOW",
    "tool": "checkov",
    "why": "CPU requests tell the Kubernetes scheduler how much CPU to reserve for the pod. Without them, the scheduler places pods randomly and nodes can become overloaded.",
    "fix": "Set CPU requests:\n  resources:\n    requests:\n      cpu: \"250m\"     # 0.25 CPU cores",
    "learnMore": "Study: Kubernetes scheduling, resource requests vs limits, cluster capacity planning"
  },
  "CKV_K8S_12": {
    "title": "Memory limits should be set",
    "severity": "MEDIUM",
    "tool": "checkov",
    "why": "Without memory limits, a memory leak in one pod can exhaust all memory on a node, causing the OS to kill random processes (OOM killer). Other pods lose data.",
    "fix": "Set memory limits:\n  resources:\n    limits:\n      memory: \"128Mi\"",
    "learnMore": "Study: Kubernetes OOM killer, memory management, container resource limits"
  },
  "CKV_K8S_13": {
    "title": "Memory requests should be set",
    "severity": "LOW",
    "tool": "checkov",
    "why": "Memory requests tell the scheduler the minimum memory a pod needs. Without them, the scheduler may place a pod on a node that cannot support it.",
    "fix": "Set memory requests:\n  resources:\n    requests:\n      memory: \"64Mi\"",
    "learnMore": "Study: Kubernetes resource requests, pod scheduling, node capacity"
  },
  "CKV_K8S_14": {
    "title": "Image should not use the :latest tag",
    "severity": "MEDIUM",
    "tool": "checkov",
    "why": "The :latest tag changes silently. A rolling update that pulls a new :latest may introduce a breaking change or vulnerability. Pin to a specific immutable tag.",
    "fix": "Use a specific version tag:\n  # Bad:  image: myapp:latest\n  # Good: image: myapp:1.2.3",
    "learnMore": "Study: Image tag pinning, GitOps, immutable infrastructure"
  },
  "CKV_K8S_15": {
    "title": "Image pull policy should be Always",
    "severity": "LOW",
    "tool": "checkov",
    "why": "IfNotPresent means Kubernetes will use a cached old image if one exists. If you push a security fix with the same tag, nodes that already have the old image won't pull the fix.",
    "fix": "Set imagePullPolicy:\n  imagePullPolicy: Always",
    "learnMore": "Study: Kubernetes image pull policies, rolling deployments, image caching"
  },
  "CKV_K8S_16": {
    "title": "Container should not run as root",
    "severity": "HIGH",
    "tool": "checkov",
    "why": "A root container can read sensitive host files, modify the filesystem, and escalate privileges outside the container if the kernel has a vulnerability.",
    "fix": "Set runAsNonRoot in the security context:\n  securityContext:\n    runAsNonRoot: true\n    runAsUser: 1000",
    "learnMore": "Study: Kubernetes security context, container isolation, privilege escalation"
  },
  "CKV_K8S_28": {
    "title": "Minimise containers with added capabilities",
    "severity": "HIGH",
    "tool": "checkov",
    "why": "Linux capabilities break down root privileges into smaller units. Adding extra capabilities (like NET_ADMIN or SYS_PTRACE) gives an attacker more attack surface if they break into the container.",
    "fix": "Drop all capabilities and only add back what you need:\n  securityContext:\n    capabilities:\n      drop: [\"ALL\"]\n      add: [\"NET_BIND_SERVICE\"]  # only if needed",
    "learnMore": "Study: Linux capabilities, principle of least privilege, container security"
  },
  "CKV_K8S_30": {
    "title": "Apply security context to your pod",
    "severity": "MEDIUM",
    "tool": "checkov",
    "why": "Without a security context, containers run with default (often too permissive) settings. A security context explicitly defines what the container can and cannot do.",
    "fix": "Add securityContext at both pod and container level:\n  spec:\n    securityContext:\n      runAsNonRoot: true\n      runAsUser: 1000\n    containers:\n    - securityContext:\n        readOnlyRootFilesystem: true\n        allowPrivilegeEscalation: false",
    "learnMore": "Study: Kubernetes Pod Security Standards, security context, seccomp profiles"
  },
  "CKV_TF_1": {
    "title": "Terraform module source should use a commit hash",
    "severity": "MEDIUM",
    "tool": "checkov",
    "why": "Module sources using branch names (ref=main) are mutable — the module code can change without you knowing. A commit hash is immutable and guarantees you always get the same code.",
    "fix": "Pin module to a commit hash:\n  source = \"git::https://github.com/org/module.git?ref=a1b2c3d\"",
    "learnMore": "Study: Supply chain security, Terraform module pinning, GitOps"
  },
  "CKV_AZURE_1": {
    "title": "Azure Storage Account should enforce HTTPS",
    "severity": "HIGH",
    "tool": "checkov",
    "why": "HTTP transmits data in plaintext. Anyone who can intercept network traffic between your app and storage can read or modify the data.",
    "fix": "Set enable_https_traffic_only = true:\n  resource \"azurerm_storage_account\" \"example\" {\n    enable_https_traffic_only = true\n    min_tls_version           = \"TLS1_2\"\n  }",
    "learnMore": "Study: Transport Layer Security, man-in-the-middle attacks, Azure storage encryption"
  },
  "CKV_AZURE_2": {
    "title": "Azure managed disk should be encrypted",
    "severity": "HIGH",
    "tool": "checkov",
    "why": "Unencrypted managed disks can be read directly if an attacker gains access to the underlying storage. Encryption at rest protects data from physical and logical storage attacks.",
    "fix": "Enable encryption with a key:\n  resource \"azurerm_managed_disk\" \"example\" {\n    encryption_settings {\n      enabled = true\n    }\n  }",
    "learnMore": "Study: Azure disk encryption, encryption at rest, key management"
  },
  "runAsNonRoot": {
    "title": "Pod must not run as root",
    "severity": "HIGH",
    "tool": "polaris",
    "why": "If a container runs as root (UID 0) and an attacker exploits a vulnerability in your application, they get root access inside the container — potentially allowing host escape.",
    "fix": "Add to spec.securityContext or container.securityContext:\n  securityContext:\n    runAsNonRoot: true\n    runAsUser: 1000",
    "learnMore": "Study: Kubernetes Pod Security Standards, user namespaces, privilege escalation"
  },
  "readOnlyRootFilesystem": {
    "title": "Container filesystem should be read-only",
    "severity": "MEDIUM",
    "tool": "polaris",
    "why": "A writable root filesystem lets malware write scripts or modify binaries inside the container. Read-only root filesystem is a key defence against many attack patterns.",
    "fix": "Set readOnlyRootFilesystem in the container security context:\n  securityContext:\n    readOnlyRootFilesystem: true\n  # Mount a writable volume for runtime data:\n  volumeMounts:\n  - name: tmp\n    mountPath: /tmp",
    "learnMore": "Study: Immutable infrastructure, container hardening, Kubernetes security context"
  },
  "cpuRequestsMissing": {
    "title": "CPU requests not set",
    "severity": "LOW",
    "tool": "polaris",
    "why": "The Kubernetes scheduler uses resource requests to decide which node to place a pod on. Without CPU requests, pods may be placed on overloaded nodes.",
    "fix": "resources:\n  requests:\n    cpu: \"250m\"",
    "learnMore": "Study: Kubernetes resource requests, scheduler, QoS classes"
  },
  "cpuLimitsMissing": {
    "title": "CPU limits not set",
    "severity": "MEDIUM",
    "tool": "polaris",
    "why": "Without CPU limits, a single runaway pod can consume all CPU on a node, affecting every other pod running there.",
    "fix": "resources:\n  limits:\n    cpu: \"500m\"",
    "learnMore": "Study: Kubernetes CPU throttling, resource limits, noisy neighbour problem"
  },
  "memoryRequestsMissing": {
    "title": "Memory requests not set",
    "severity": "LOW",
    "tool": "polaris",
    "why": "Without memory requests, Kubernetes cannot guarantee the pod gets the memory it needs. On a constrained node, the pod may be OOM killed.",
    "fix": "resources:\n  requests:\n    memory: \"64Mi\"",
    "learnMore": "Study: Kubernetes memory management, OOM killer, QoS classes"
  },
  "memoryLimitsMissing": {
    "title": "Memory limits not set",
    "severity": "MEDIUM",
    "tool": "polaris",
    "why": "A memory leak without limits will grow until the OS kills processes on the node, causing cascading failures. Limits cap the damage.",
    "fix": "resources:\n  limits:\n    memory: \"128Mi\"",
    "learnMore": "Study: Container memory limits, OOM killer, Kubernetes resource governance"
  },
  "livenessProbeMissing": {
    "title": "Liveness probe not configured",
    "severity": "MEDIUM",
    "tool": "polaris",
    "why": "Without a liveness probe, a deadlocked application keeps running forever. Kubernetes has no way to know it should restart the pod.",
    "fix": "livenessProbe:\n  httpGet:\n    path: /health\n    port: 3000\n  initialDelaySeconds: 15\n  periodSeconds: 20",
    "learnMore": "Study: Kubernetes self-healing, liveness vs readiness probes, health check endpoints"
  },
  "readinessProbeMissing": {
    "title": "Readiness probe not configured",
    "severity": "MEDIUM",
    "tool": "polaris",
    "why": "Without a readiness probe, traffic is sent to a pod that hasn't finished starting up. Users see 503 errors during deployments.",
    "fix": "readinessProbe:\n  httpGet:\n    path: /ready\n    port: 3000\n  initialDelaySeconds: 5\n  periodSeconds: 10",
    "learnMore": "Study: Kubernetes rolling updates, zero-downtime deployments, readiness gates"
  },
  "pullPolicyNotAlways": {
    "title": "Image pull policy not set to Always",
    "severity": "LOW",
    "tool": "polaris",
    "why": "IfNotPresent may reuse a cached old image after you push a security fix. Always ensures the latest image is pulled on every pod start.",
    "fix": "imagePullPolicy: Always",
    "learnMore": "Study: Kubernetes image lifecycle, rolling updates, supply chain security"
  },
  "tagNotSpecified": {
    "title": "Image using :latest or no tag specified",
    "severity": "MEDIUM",
    "tool": "polaris",
    "why": "Untagged or :latest images are not reproducible. You cannot roll back to a known-good state because :latest is a moving target.",
    "fix": "Pin to an immutable tag:\n  image: myapp:1.2.3  # or a SHA digest: myapp@sha256:abc...",
    "learnMore": "Study: Image immutability, GitOps, Kubernetes deployment strategies"
  },
  "insecureCapabilities": {
    "title": "Dangerous Linux capabilities added to container",
    "severity": "HIGH",
    "tool": "polaris",
    "why": "Capabilities like SYS_ADMIN, NET_ADMIN, and SYS_PTRACE give broad kernel-level access. If an attacker exploits your app, they can use these capabilities to escalate to the host.",
    "fix": "Drop ALL capabilities and only add back what is strictly needed:\n  securityContext:\n    capabilities:\n      drop: [\"ALL\"]\n      add: [\"NET_BIND_SERVICE\"]",
    "learnMore": "Study: Linux capabilities man page, container privilege escalation, CAP_SYS_ADMIN risks"
  },
  "hostIPCSet": {
    "title": "hostIPC should not be true",
    "severity": "HIGH",
    "tool": "polaris",
    "why": "hostIPC: true gives the container access to the host's IPC namespace. It can read shared memory from other processes on the host, including other containers.",
    "fix": "Remove or set to false:\n  spec:\n    hostIPC: false",
    "learnMore": "Study: Linux IPC namespaces, container isolation, Kubernetes security"
  },
  "hostPIDSet": {
    "title": "hostPID should not be true",
    "severity": "HIGH",
    "tool": "polaris",
    "why": "hostPID: true lets the container see and signal all processes on the host. An attacker could kill critical system processes or inspect sensitive process data.",
    "fix": "Remove or set to false:\n  spec:\n    hostPID: false",
    "learnMore": "Study: Linux PID namespaces, container isolation, privilege escalation"
  },
  "hostNetworkSet": {
    "title": "hostNetwork should not be true",
    "severity": "HIGH",
    "tool": "polaris",
    "why": "hostNetwork: true removes the network namespace isolation. The container shares the host's network stack and can listen on and connect to any host port.",
    "fix": "Remove or set to false:\n  spec:\n    hostNetwork: false",
    "learnMore": "Study: Linux network namespaces, pod network isolation, Kubernetes CNI"
  },
  "privilegeEscalationAllowed": {
    "title": "allowPrivilegeEscalation should be false",
    "severity": "HIGH",
    "tool": "polaris",
    "why": "If allowPrivilegeEscalation is true (the default), a process inside the container can use setuid binaries to gain more privileges than its parent — potentially becoming root.",
    "fix": "Explicitly disable it:\n  securityContext:\n    allowPrivilegeEscalation: false",
    "learnMore": "Study: Linux setuid, privilege escalation, Kubernetes pod security standards"
  }
}
